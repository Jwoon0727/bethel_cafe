# 대량 주문 부하 · 데이터 정합성 검토 보고서

> **요청**: "2시간 동안 많은 수의 주문이 있을 예정. DB 꼬임 가능성을 검증하고 발생 가능한 이슈를 정리."
> **대상**: `cafe_order` — Next.js 16.3.3 (App Router) + Supabase(anon 키 + RPC 직접 호출)
> **검토 범위**: `supabase/schema.sql`, `src/lib/orders.ts`, `src/app/page.tsx`, `src/app/kitchen/page.tsx`, `src/app/display/page.tsx`
> **작성일**: 2026-08-31 · **검토 방식**: 정적 코드/스키마 분석 (실부하 테스트 미수행)

---

## 0. 결론 요약

**DB(테이블 상태) 자체가 꼬일 위험은 낮습니다.** 순번 발급과 상태 전이는 동시성 관점에서 올바르게 설계되어 있습니다 (근거는 8장).

그러나 **행사를 망칠 수 있는 위험은 DB가 아니라 클라이언트 화면과 권한 설정에 있습니다.**

| # | 이슈 | 심각도 | 유형 |
|---|------|--------|------|
| R-01 | `reset_all_orders`가 `anon`에게 열려 있고 PIN이 클라이언트 상수 | **Critical** | 보안 / 데이터 전손 |
| R-02 | Kitchen 화면 Realtime 끊김 시 복구 로직 없음 (조용히 정지) | **High** | 운영 중단 |
| R-03 | Kitchen이 주문 전체를 limit 없이 로드 → 2시간 누적 시 화면 지연 | **High** | 성능 |
| R-04 | 주문 생성 재시도 시 중복 주문 (멱등키 없음) | **High** | 데이터 정합성 |
| R-05 | 행사 중 초기화 실행 시 순번 중복으로 주문 실패 가능 | **Medium** | 데이터 정합성 |
| R-06 | 자동 픽업(3분) 타이머와 Undo의 경합 — 취소가 되돌아감 | **Medium** | 운영 혼선 |
| R-07 | `create_order` 무제한 익명 호출 (주문 스팸) | **Medium** | 보안 |
| R-08 | 한 주문 최대 396잔 (수량 상한만 있고 총량 상한 없음) | **Low** | 입력 검증 |

---

## 1. R-01 — `reset_all_orders`가 익명에게 열려 있음 (Critical)

**근거**
- `supabase/schema.sql`: `grant execute on function public.reset_all_orders() to anon, authenticated;`
- `src/app/kitchen/page.tsx:18`: `const RESET_PIN = "1914";`

**문제**

PIN 검증이 **브라우저 JS 안에서만** 이뤄집니다. `RESET_PIN`은 빌드된 번들에 문자열 그대로 들어가므로 누구나 볼 수 있고, 애초에 PIN을 몰라도 됩니다 — Supabase URL과 anon 키(둘 다 `NEXT_PUBLIC_*`이라 공개)만 있으면 아래 한 줄로 **전체 주문과 순번이 삭제**됩니다.

```
POST https://<project>.supabase.co/rest/v1/rpc/reset_all_orders
apikey: <NEXT_PUBLIC_SUPABASE_ANON_KEY>
```

**영향**

행사 도중 실행되면 진행 중인 모든 주문이 사라지고 카운터가 0으로 돌아갑니다. 되돌릴 수 없습니다. 손님은 A7 번호표를 들고 있는데 주방 화면은 비어 있는 상태가 됩니다.

**권장 조치(제안)**
- 초기화는 anon에서 회수하고 `authenticated` 또는 Supabase Dashboard SQL Editor 수동 실행으로 한정
- 최소한 RPC 인자로 PIN을 받아 **DB 함수 안에서** 검증
- 행사 당일에는 아예 `revoke execute ... from anon` 상태로 두고, 종료 후 대시보드에서 초기화

---

## 2. R-02 — Kitchen Realtime이 끊기면 조용히 멈춤 (High)

**근거**
- `src/app/kitchen/page.tsx:52-77` — `.subscribe()`에 상태 콜백 없음, 폴링 폴백 없음
- 대조군: `src/app/display/page.tsx:68-77` — 재연결(`RECONNECT_DELAY_MS`) + 60초 폴링 폴백 **있음**

**문제**

Display 화면에는 있는 두 가지 안전장치가 Kitchen에는 없습니다. 채널이 `CHANNEL_ERROR` / `TIMED_OUT` / `CLOSED` 상태가 되어도 **아무 일도 일어나지 않고**, 화면은 마지막으로 받은 목록에서 멈춥니다. 에러 표시도 없습니다.

**영향**

행사장 Wi-Fi에서 2시간이면 연결 끊김은 사실상 확정적입니다. 주방은 화면이 멈춘 줄 모르고 "주문이 안 들어오네" 하며 대기하게 되고, 손님 주문은 DB에는 정상 기록되지만 아무도 만들지 않습니다. **운영 사고 발생 확률 1순위입니다.**

**권장 조치(제안)**
- Display와 동일한 재연결 + 주기 폴링(30~60초) 적용
- 연결 상태를 헤더에 표시 (연결됨/끊김) — 직원이 눈으로 알 수 있게
- 임시 대응: 직원에게 "10분마다 새로고침" 안내 (이미 새로고침 버튼 존재)

---

## 3. R-03 — Kitchen이 주문 전체를 로드 (High)

**근거**
- `src/app/kitchen/page.tsx:35-38`
  ```
  .from("orders").select("*").order("created_at", { ascending: true })
  ```
  `limit` 없음. `picked_up`(수령완료)도 제외하지 않음.

**문제**

수령완료 주문이 계속 누적되고, 새로고침할 때마다 **행사 시작부터의 전 주문**을 다시 받아 전부 렌더링합니다. 3열 중 "수령완료" 열에 수백~수천 장의 카드가 쌓입니다.

추가로 `sortByCreatedAt([...prev, row])` (page.tsx:61)는 INSERT 이벤트마다 전체 배열을 복사·정렬합니다. 주문 n건일 때 이벤트당 O(n log n)이 되어, 후반부로 갈수록 새 주문이 뜨는 속도가 느려집니다.

**영향**

2시간 대량 주문 기준으로 태블릿 등 저사양 기기에서는 후반부에 화면이 버벅이거나 멈출 수 있습니다. **"DB 꼬임"보다 이쪽이 먼저 터질 가능성이 높습니다.**

**권장 조치(제안)**
- 초기 로드에 `limit` + 최근 N시간 필터 적용
- 수령완료 열은 최근 20~30건만 유지 (Display의 `MAX_ORDERS` 방식과 동일)
- 통계(`pickedUpStats`)는 화면 목록과 분리해 별도 집계 쿼리로

---

## 4. R-04 — 재시도 시 중복 주문 (High)

**근거**
- `src/lib/orders.ts:8-30` — `createOrder`에 멱등키(idempotency key) 없음
- `src/app/page.tsx` `handleOrder` — `submitting` 플래그는 **같은 탭 안에서만** 이중 클릭을 막음

**문제**

네트워크가 느려 요청이 타임아웃되면, 사용자 화면에는 에러가 뜨지만 **DB에는 이미 INSERT가 완료되어 있을 수 있습니다.** 사용자가 "주문 등록"을 다시 누르면 같은 주문이 두 번 들어가고 번호도 두 개 발급됩니다.

**영향**

접수대 직원 입장에서는 "실패했다고 나와서 다시 눌렀는데 주방에 두 번 떴다" — 음료 과잉 제조. 대량 주문 + 불안정한 회선 조합에서 현실적으로 발생합니다.

**권장 조치(제안)**
- 클라이언트에서 주문마다 UUID(`client_request_id`)를 생성해 RPC 인자로 전달
- `orders`에 해당 컬럼 + unique 제약을 두고, `create_order`에서 이미 존재하면 기존 행을 반환 (재시도가 안전해짐)
- 최소 대응: 실패 메시지에 "주방 화면에서 번호를 확인한 뒤 재시도" 안내 문구 추가

---

## 5. R-05 — 행사 중 초기화 시 순번 충돌 (Medium)

**시나리오**

`reset_all_orders`는 `delete from orders` → `update order_counters set last_no = 0` 순으로 실행됩니다. 이 두 문장 사이에 다른 `create_order`가 커밋되면, **삭제되지 않고 살아남은 주문(예: A6)** 이 남은 상태에서 카운터만 0이 됩니다.

이후 주문은 A1, A2… 로 다시 발급되고, A6에 도달하는 순간 `constraint orders_station_no_uniq unique (station, order_no)` 위반으로 **주문 생성이 실패**합니다. 접수대에서는 원인 불명의 에러가 반복됩니다.

**영향**

DB가 실제로 "꼬이는" 유일한 확인된 시나리오입니다. 다만 초기화를 누르지 않으면 발생하지 않습니다.

**권장 조치(제안)**
- **행사 중에는 초기화 버튼을 절대 사용하지 않는다** (운영 수칙으로 명시)
- R-01 조치로 anon 권한을 회수하면 실수 여지도 함께 사라짐

---

## 6. R-06 — 자동 픽업 타이머 vs 취소(Undo) 경합 (Medium)

**근거**
- `src/app/kitchen/page.tsx:203-232` — 3분 자동 픽업 타이머
- `src/app/kitchen/page.tsx:162` — `revertGuardRef`는 **해당 브라우저 탭 안에만 존재**하는 로컬 가드

**문제**

주방 화면을 2대 이상(태블릿 + 노트북 등) 띄우면 각 기기가 **독립적으로** 3분 타이머를 돌립니다. 직원이 A기기에서 "수령완료 → 취소"를 눌러도, B기기의 `revertGuardRef`는 비어 있습니다.

`revert_order_to_done` RPC가 `done_at = now()`로 갱신하므로 **대부분은 Realtime UPDATE를 통해 방어됩니다.** 다만 R-02(Realtime 끊김)가 함께 발생한 기기에서는 옛 `done_at`을 보고 있어, 취소한 주문을 즉시 다시 수령완료로 넘겨버립니다.

**영향**

"취소를 눌러도 자꾸 되돌아간다"는 현상. 데이터가 손상되지는 않습니다 (RPC의 `where status = '이전상태'` 가드 덕분).

**부가 관찰** — `useEffect([orders])`가 주문 배열이 바뀔 때마다 모든 타이머를 해제·재생성합니다. 남은 시간은 `done_at` 기준으로 매번 재계산되므로 시각이 밀리지는 않지만, 대량 주문 구간에서는 이벤트마다 수백 개의 `setTimeout`을 재등록하게 됩니다.

**권장 조치(제안)**
- 자동 픽업을 클라이언트 타이머가 아니라 DB 측(예: `done_at < now() - interval '3 minutes'` 조건의 일괄 처리 RPC)에서 처리
- 또는 주방 화면을 **1대만** 운영하도록 수칙화

---

## 7. R-07 · R-08 — 익명 주문 스팸 / 총량 미제한 (Medium · Low)

- **R-07**: `create_order`가 `anon`에 열려 있고 rate limit이 없습니다. 주문 URL이 외부에 알려지면 누구나 무제한으로 주문을 생성할 수 있습니다. 주방 화면이 가짜 주문으로 덮이면 R-03과 겹쳐 사실상 마비됩니다.
  → 조치 제안: Supabase 프로젝트의 rate limit 설정 확인, 또는 접수대에서만 접근 가능한 경로/키 분리
- **R-08**: `create_order`는 항목당 수량을 1~99로 제한하지만 **주문 전체 총량 상한이 없습니다.** 메뉴 4종 × 99 = 한 주문에 396잔이 가능합니다. 항목 배열의 중복 `code` 검증도 없어 같은 메뉴가 두 줄로 들어올 수 있습니다.
  → 조치 제안: 총 수량 상한(예: 30잔) + `code` 중복 검사 추가

---

## 8. DB 꼬임 검증 — 안전하다고 판단한 부분

아래 항목들은 **동시성 문제가 없다고 판단**했으며, 근거를 함께 적습니다.

### 8.1 순번 중복/누락 — 안전 ✅
```sql
update public.order_counters
   set last_no = last_no + 1
 where station = p_station
returning last_no into v_no;
```
같은 station에 대한 동시 요청은 PostgreSQL의 행 단위 잠금으로 자동 직렬화됩니다. 두 요청이 같은 번호를 받는 일은 없습니다. `unique (station, order_no)` 제약이 2차 방어선으로 존재합니다.

트랜잭션이 롤백되면 카운터 증가도 함께 롤백되므로(시퀀스와 달리) **번호에 구멍이 생기지 않습니다.** 접수대를 A/B/C로 나눈 설계가 잠금 경합도 1/3로 분산시킵니다. 2시간 규모 트래픽에서 이 잠금이 병목이 될 가능성은 낮습니다.

### 8.2 상태 전이 중복 적용 — 안전 ✅
모든 상태 변경 RPC가 `where id = p_id and status = '이전상태'` 조건을 갖습니다. 이중 클릭, 여러 기기 동시 클릭, 자동 타이머 중복 실행 어느 경우에도 **최초 1회만 반영**되고 나머지는 0행을 반환합니다. `done_at` / `picked_at`이 덮어써지지 않습니다.

### 8.3 직접 쓰기 차단 — 안전 ✅
`orders`·`order_counters` 모두 RLS 활성화. `orders`에는 SELECT 정책만 존재하고 INSERT/UPDATE/DELETE 정책이 없어 기본 거부됩니다. `order_counters`는 정책이 전혀 없어 전면 차단입니다. 모든 쓰기는 `security definer` RPC를 경유합니다.

### 8.4 `security definer` 함수의 search_path — 안전 ✅
모든 RPC에 `set search_path = public`이 지정되어 있어, search_path 조작을 통한 권한 상승 공격이 차단됩니다.

### 8.5 조회 인덱스 — 대체로 적절 ⚠️
`orders_pending_idx`(pending, created_at)와 `orders_done_idx`(done, done_at desc) 부분 인덱스가 존재해 Display 조회는 효율적입니다. 다만 Kitchen의 전체 조회(R-03)는 이 인덱스를 타지 못하고 전체 스캔 + 정렬을 수행합니다.

### 8.6 개인정보 — 없음 ✅
`orders`에는 접수대/번호/메뉴/상태만 저장되며 개인 식별 정보가 없습니다. 공개 SELECT 정책이 정보 유출로 이어지지 않습니다.

---

## 9. 확인이 필요한 항목 (정적 분석만으로는 단정 불가)

| 항목 | 확인 방법 |
|------|-----------|
| Supabase Realtime 동시 접속 수 / 초당 메시지 제한 — 대량 주문 시 스로틀 여부 | 프로젝트 플랜의 Realtime 쿼터 확인 |
| `language sql` RPC가 0행일 때 PostgREST 응답이 `null`인지, 전 필드 NULL 행인지 | 실제 호출 1회로 확인 (후자면 `markOrderDone`의 null 판정이 깨짐) |
| 2시간 예상 주문 건수 · 동시 접수대 수 | 미확정 — 수치가 나오면 R-03 상한값 산정 가능 |
| 실부하 하에서의 응답 지연 | 부하 테스트 미수행 |

---

## 10. 행사 당일 운영 체크리스트

**사전 (코드 수정 없이 가능한 것)**
- [ ] 초기화는 행사 **시작 전에만** 1회 실행하고, 이후 버튼을 누르지 않기로 합의 (R-05)
- [ ] 가능하면 `reset_all_orders`의 anon 실행 권한을 대시보드에서 회수 (R-01)
- [ ] 주방 화면은 **1대만** 운영 (R-06)
- [ ] 주문 URL을 외부에 공개하지 않기 (R-07)

**진행 중**
- [ ] 주방 담당자가 10~15분마다 새로고침 (R-02 임시 대응)
- [ ] "실패" 메시지가 떠도 곧바로 재클릭하지 말고, 주방 화면에서 번호 유입 여부를 먼저 확인 (R-04)
- [ ] 후반부에 화면이 느려지면 새로고침 (R-03)

**사후**
- [ ] 대시보드에서 주문 데이터 백업(export) 후 초기화

---

## 11. 조치 우선순위 (구현은 미수행)

| 순위 | 항목 | 사유 |
|------|------|------|
| 1 | R-02 Kitchen Realtime 폴백 | 발생 확률 최고, Display에 이미 있는 코드를 옮기면 됨 |
| 2 | R-01 초기화 권한 회수 | 피해 규모 최대, 대시보드 설정만으로 즉시 완화 가능 |
| 3 | R-03 Kitchen 로드 상한 | 후반부 사용성 직결 |
| 4 | R-04 멱등키 | 스키마 변경 필요 — 시간이 있을 때 |
| 5 | R-06~R-08 | 운영 수칙으로 우회 가능 |

---

*본 보고서는 정적 분석 결과이며 실부하 테스트를 포함하지 않습니다. 조치는 제안 단계이며 코드 수정은 수행하지 않았습니다.*
